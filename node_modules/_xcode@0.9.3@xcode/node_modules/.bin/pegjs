#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_pegjs@0.10.0@pegjs/bin/pegjs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_pegjs@0.10.0@pegjs/bin/pegjs" "$@"
  ret=$?
fi
exit $ret
