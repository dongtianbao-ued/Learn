#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_detect-newline@1.0.3@detect-newline/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_detect-newline@1.0.3@detect-newline/cli.js" "$@"
  ret=$?
fi
exit $ret
